export interface GlobalConfig {
  SOUNDS_URL: string
}

export interface Sound {
  url: string
  datetime: string
  title: string
  artist: string
  performer: string
  status: number
  source: string
}
